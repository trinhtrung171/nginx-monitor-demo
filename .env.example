# Docker credentials (for pushing to Docker Hub)
DOCKERHUB_USERNAME=your-dockerhub-username

# Gmail SMTP Configuration for Alertmanager
SMTP_USERNAME=your-email@gmail.com
SMTP_PASSWORD=your-app-password
SMTP_FROM=your-email@gmail.com
SMTP_TO=recipient@example.com

# Grafana Admin Password
GF_PASSWORD=admin

# Metrics authentication token for prom-client /metrics endpoint
METRICS_AUTH_TOKEN=generate-with-openssl-rand-hex-32

# Public URL of your Vercel-deployed app (include https://)
VERCEL_URL=https://your-app.vercel.app

# (Optional) OpenTelemetry Configuration — removed; using prom-client instead
